// JavaScript keeps track of what function is currently
// running (where’s the thread of execution)
// - Run a function - add to call stack
// - Finish running the function - JS removes it from
// call stack
// - Whatever is top of the call stack - that’s the
// function we’re currently running

for (let index = 0; index < array.length; index++) {
  const element = array[index];
}
